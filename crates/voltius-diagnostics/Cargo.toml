[package]
name = "voltius-diagnostics"
version = "0.1.0"
edition = "2021"
description = "Pure-Rust diagnostics helpers (secret redaction + report bundling) for Voltius"
license = "AGPL-3.0"

[dependencies]
regex = "1"
zip = { version = "2", default-features = false, features = ["deflate"] }
